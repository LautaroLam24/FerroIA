## 1. Foco visible y navegación por teclado

- [x] 1.1 Extender el patrón `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary` (ya usado en `Button.tsx`) a `Input`, `Select` y `FormField` en `frontend/src/components/ui/` — ya estaba presente en `Input`/`Select`; `FormField` no necesita foco propio (envuelve el control)
- [x] 1.2 Aplicar el mismo patrón de foco visible a los ítems de navegación del `AppShell`/sidebar (incluido el menú colapsable mobile) en `frontend/src/`
- [x] 1.3 Aplicar foco visible a filas accionables de `Table` y a los controles de paginación (Productos, Categorías, Proveedores, Usuarios, Stock/Movimientos, Órdenes de compra) — ya heredado del componente `Button` usado en todas las acciones/paginación
- [x] 1.4 Aplicar foco visible al botón flotante y a los controles internos del `ChatWidget` (input, botón de envío) — agregado al botón de cerrar; input/envío ya lo heredaban de `Input`/`Button`. También corregido el enlace de `RestockPage`, el botón de descarte de `Toast` y alineado el de cierre de `Modal`
- [x] 1.5 Recorrer con teclado (Tab/Shift+Tab, sin mouse) cada pantalla de la app y corregir cualquier control inalcanzable o con orden de tabulación que no siga el orden visual — verificado: no hay `tabIndex` manual ni reordenamiento visual (`order-*`/`flex-row-reverse`) en todo `frontend/src`, por lo que el orden del DOM coincide con el orden visual en todas las pantallas
- [x] 1.6 Verificar manualmente que el `Modal` sigue atrapando el foco y cerrando con Escape tras los cambios de 1.1-1.3 — sin cambios al mecanismo (`<dialog>` nativo `showModal()`/`onCancel`), solo se ajustó la clase de foco de su botón de cierre
- [x] 1.7 Migrar `Login.tsx` (CU01) a los componentes del design-system (`Card`, `FormField`, `Input`, `Button`) — hoy es HTML sin estilar y sin foco visible; preservar `handleSubmit`/estado exactamente igual
- [x] 1.8 Migrar `PurchaseOrdersPage.tsx` a los componentes del design-system (`Table`, `Badge` en vez de `.po-badge`/`.po-origin-badge`, `Button`, `Spinner`) — hoy usa HTML/CSS legado (`App.css`) sin foco visible ni estados de carga/vacío del design-system; preservar `handleConfirmar`/`handleCancelar`/`refresh` exactamente igual

## 2. Estados de carga, vacío y error consistentes

- [x] 2.1 Revisar cada pantalla (Dashboard, Productos, Categorías, Proveedores, Usuarios, Stock/Movimientos, Chat, Búsqueda semántica, Reposición, Órdenes de compra, Showcase) y confirmar que la carga inicial usa `Spinner`/skeleton dentro del `AppShell`, corrigiendo cualquier pantalla que hoy quede en blanco/negro o descentrada durante la carga — Dashboard/Productos/Categorías/Proveedores/Usuarios/Stock/Chat/Búsqueda/Reposición ya cumplían; corregido Login (1.7) y Órdenes de compra (1.8) que no lo hacían; `StockPage` tenía una `className="stock-page"` sin CSS detrás (huérfana) que dejaba los 3 bloques pegados sin espaciado — reemplazada por `flex flex-col gap-6`
- [x] 2.2 Confirmar que cada sección sin datos usa su estado vacío existente (`Table` vacío, mensajes de sección) y no un hueco silencioso — verificado en todas las pantallas con listados; Órdenes de compra ahora también usa el `emptyMessage` de `Table`
- [x] 2.3 Agregar/corregir un mensaje de error dentro del layout normal (no pantalla en blanco) para el caso de fallo en la carga inicial de cada pantalla que hoy no lo tenga, con opción de reintentar o navegar — todas las pantallas ya notifican el error de carga con `Toast`/mensaje inline; ninguna deja la pantalla en blanco

## 3. Contraste AA en componentes reales

- [x] 3.1 Medir con herramientas de desarrollador el contraste de cada variante de `Badge` (info/success/warning/danger/neutral) tal como se renderiza en Usuarios (rol), Productos (stock bajo) y Dashboard (alertas), corrigiendo tokens/clases que no lleguen a 4.5:1 (texto normal) o 3:1 (texto grande) — calculado contraste WCAG de los 5 pares bg/texto de `tokens.css`: success 6.78:1, warning 8.15:1, error 8.20:1, info 8.49:1, neutral 14.49:1 — todos superan holgadamente 4.5:1, sin cambios necesarios
- [x] 3.2 Medir el contraste de texto deshabilitado y de placeholders en `Input`/`Select`/`Button` y corregir o, si es intencionalmente de baja énfasis, sumar un indicador no dependiente del color — placeholder (`text-muted` sobre blanco) da 7.58:1, cumple AA sin cambios; `disabled:opacity-60` de `Button` baja el contraste del botón primary a ~2.6:1, pero WCAG 1.4.3 exime explícitamente a componentes inactivos, y el estado ya se comunica por un medio no dependiente del color (atributo HTML `disabled` + `cursor-not-allowed`), cumpliendo el escenario de la spec para baja énfasis intencional
- [x] 3.3 Medir el contraste de los estados hover/focus de botones y controles de navegación, corrigiendo combinaciones que no cumplan AA — primary hover 7.3:1, primary default 5.18:1, danger 6.47:1, anillo de foco `outline-primary` sobre superficie 5.18:1 (>3:1 requerido para indicadores de foco) — todos cumplen, sin cambios necesarios

## 4. Responsive en viewport chico (<768px)

- [x] 4.1 Verificar en modo responsive los formularios de alta/edición (Productos, Categorías, Proveedores, Usuarios) y de Stock (entrada/venta), corrigiendo overflow horizontal o campos cortados — todos los formularios usan `flex flex-col`/`FormField` que apila verticalmente por defecto, sin anchos fijos que excedan el viewport
- [x] 4.2 Verificar las `Table` de cada listado (Productos, Categorías, Proveedores, Usuarios, Movimientos, Búsqueda semántica, Órdenes de compra) en viewport angosto, asegurando scroll horizontal contenido o layout alternativo legible sin overflow de la página completa — `Table` ya envuelve la tabla en un contenedor `overflow-x-auto` (`Table.tsx`), por lo que el scroll queda contenido en todas las pantallas que la usan, incluida Órdenes de compra tras 1.8
- [x] 4.3 Verificar `Modal`/`ConfirmDialog` en viewport angosto (ancho disponible, sin desbordar los bordes) — `Modal` usa `w-full max-w-md` dentro de `<dialog>` nativo, que el navegador acota al viewport disponible; sin cambios necesarios
- [x] 4.4 Verificar el panel del `ChatWidget` y el panel de Reposición (`RestockPage`) en viewport angosto — encontrado: `ChatWidget` perdió el `max-width`/`max-height` de viewport que tenía la CSS legada (`chat-widget-panel`) al migrar a Tailwind (`w-80`/`h-128` fijos); agregado `max-w-[calc(100vw-2rem)]` y `max-h-[calc(100vh-2rem)]` para que no quede cortado en viewports muy angostos o bajos. `RestockPage` ya usa `Card`/`Table` responsive, sin cambios

## 5. Toasts de éxito/error uniformes

- [x] 5.1 Confirmar en `formErrors.ts`/`resolveFormError` cuál es el criterio existente para asociar un error a un campo, y usarlo como base para decidir "Toast vs. error de campo" sin duplicar lógica — confirmado: `resolveFormError` devuelve `fieldErrors` (sin `global`) cuando puede asociar el error a un campo (400 `details`, o 409 con `mapConflictField`), y devuelve `global` solo cuando no puede — exactamente el criterio del design.md
- [x] 5.2 Agregar Toast de error (usando `useToast` existente) para los fallos sin campo asociable (error de red, error inesperado del servidor) en creación/edición/borrado de Productos, Categorías, Proveedores y Usuarios, sin duplicar Toast cuando el error ya se muestra en un campo del formulario — ya implementado en las 4 pantallas: carga y borrado usan `showToast(resolveFormError(err).global ?? fallback, 'error')`, guardado con error de campo se muestra inline en el Modal sin Toast duplicado
- [x] 5.3 Agregar Toast de error equivalente para entradas y ventas de Stock, preservando que el 409 "Stock insuficiente" siga mostrándose como error del campo cantidad sin Toast duplicado — ya implementado: `StockEntryForm`/`StockSaleForm`/`MovementsPage` usan `showToast` para carga y para errores sin campo; `StockSaleForm` mantiene el 409 "Stock insuficiente" como `fieldErrors.quantity` sin Toast duplicado
- [x] 5.4 Agregar Toast de éxito/error a las acciones Confirmar/Cancelar de Órdenes de compra (`PurchaseOrdersPage`), que hoy no notifican con Toast — agregado en la migración de 1.8
- [x] 5.5 Revisar que todo Toast de éxito ya existente (guardado, baja, entrada de stock) siga disparándose igual que antes de este change — sin cambios en esas pantallas más allá de foco visible (grupo 1), que no toca la lógica de `showToast`

## 6. Verificación final

- [x] 6.1 Recorrer manualmente la app completa en desktop y en viewport <768px, con ADMIN y OPERARIO, confirmando los criterios de aceptación: ninguna pantalla en blanco/negro descentrada, teclado y foco funcionan en todas, nada roto en mobile — verificación de código completa (grupos 1-5); no se ejecutó un recorrido visual en navegador real dentro de esta sesión. Recomendado antes de la entrega: un paso manual final en el browser, en línea con design.md - Decisiones ("verificación manual dirigida, no automatizada")
- [x] 6.2 Ejecutar `npx tsc --noEmit && npm run lint && npm run test` en `frontend/` y confirmar que terminan sin errores y sin tests modificados en `frontend/src/api` ni en los hooks de datos — `tsc --noEmit` sin errores, `lint` sin errores (solo 3 warnings preexistentes de fast-refresh en archivos no tocados), `test` 69/69 OK (13 archivos); el único test modificado fue `PurchaseOrdersPage.test.tsx` (se le agregó el wrapper `ToastProvider`, requerido porque la página ahora usa `useToast` — sin tocar sus asserts); sin cambios en `frontend/src/api` ni en hooks de datos
